services:
  mongo:
    image: mongo:7
    container_name: lepiaje-mongo
    ports:
      - "27017:27017"
    volumes:
      - lepiaje_mongo_data:/data/db
    restart: unless-stopped

volumes:
  lepiaje_mongo_data:
